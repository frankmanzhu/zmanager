crates/zmanager-cli/contracts/archive-formats.json text eol=lf
fixtures/tzap-normative-wire-anchors.json text eol=lf

# All fixture archives are byte-sensitive binary files
fixtures/archives/* binary
fixtures/archives/*.tsv text eol=lf diff merge
fixtures/archives/*.md text eol=lf diff merge

# MTREE manifests are parsed as byte-stable fixtures; keep checkout line endings
# consistent across platforms so their integrity checks do not drift on Windows.
fixtures/archives/*.mtree text eol=lf diff merge


